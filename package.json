{
  "packageManager": "pnpm@10.17.1",
  "engines": {
    "node": "24.x"
  },
  "scripts": {
    "build": "node build.mjs",
    "test": "node build.mjs && node --test",
    "type": "tsc --noEmit"
  },
  "devDependencies": {
    "esbuild": "^0.28.0",
    "jsdom": "^29.0.2",
    "typescript": "^6.0.3"
  }
}
